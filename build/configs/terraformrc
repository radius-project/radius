disable_checkpoint = true
plugin_cache_dir = "./terraform-cache"

provider_installation {
  filesystem_mirror {
    path    = "./terraform-cache/providers"
    include = ["registry.terraform.io/*/*"]
  }
  direct {
    exclude = ["registry.terraform.io/*/*"]
  }
}
