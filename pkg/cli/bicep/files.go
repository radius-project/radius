// ------------------------------------------------------------
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
// ------------------------------------------------------------

package bicep

import (
	"encoding/json"
	"fmt"
	"os"
)

//
// <AiDoc: The following comments were generated by OpenAI explaining the code as it existed at the time of this commit>
//	The function is reading a JSON file from a given file path and returning the contents of the file as a map of strings to
//	 any type. It first reads the file and then unmarshals the JSON into the map. If either of these steps fail, an error is
//	 returned.
//
// </AiDoc: End of OpenAI comments>
func ReadARMJSON(filePath string) (map[string]any, error) {
	bytes, err := os.ReadFile(filePath)
	if err != nil {
		return nil, fmt.Errorf("could not read json file: %w", err)
	}

	var template map[string]any
	err = json.Unmarshal(bytes, &template)
	if err != nil {
		return nil, fmt.Errorf("could not unmarshal json file: %w", err)
	}

	return template, nil
}
