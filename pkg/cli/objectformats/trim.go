package objectformats

import "strings"

// TrimSpaceMulti delete trailing whitespace on every line
// of the given multi-line text.
//
// This is very useful when comparing table formatted strings.
//
// <AiDoc: The following comments were generated by OpenAI explaining the code as it existed at the time of this commit>
//	
 
//	The previous function takes a string as an argument and splits it into multiple lines. It then trims any whitespace from
//	 each line and joins them back together into a single string.
//
// </AiDoc: End of OpenAI comments>
func TrimSpaceMulti(s string) string {
	lines := strings.Split(s, "\n")
	trimmed := make([]string, len(lines))

	for i, line := range lines {
		trimmed[i] = strings.TrimSpace(line)
	}
	return strings.Join(trimmed, "\n")
}
