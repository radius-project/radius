
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  name: executables.radius.dev
spec:
  group: radius.dev
  names:
    categories:
    - all
    - radius
    kind: Executable
    listKind: ExecutableList
    plural: executables
    singular: executable
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Executable is the Schema for the executables API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ExecutableSpec defines the desired state of an Executable
            type: object
            properties:
              executable:
                type: string
                description: Path to the Executable binary
              workingDirectory:
                type: string
                description: The working directory for the Executable
              args:
                type: array
                items:
                  type: string
                description: Launch arguments to be passed to the Executable
              env:
                type: object
                additionalProperties:
                  type: string
                description: Environment variables to be set for the Executable
              envFiles:
                type: array
                items:
                  type: string
                description: Environment variable files to be used for initializing Executable environment
              replicas:
                type: integer
                description: Number of replicas to launch for the Executable
                minimum: 1
                maximum: 100
                default: 1
            required:
            - executable
          status:
            description: ExecutableStatus defines the observed state of an Executable
            type: object
            properties:
              finishTimestamp:
                type: string
                format: date-time
                description: The time when ExecutableController noticed that all replicas finished running
              replicas:
                type: array
                items:
                  type: object
                  properties:
                    pid:
                      type: integer
                      description: The PID of the running process
                    exitCode:
                      type: integer
                      description: The exit code of the process
        type: object
    served: true
    storage: true
    additionalPrinterColumns:
    - jsonPath: .spec.executable
      name: Executable
      type: string
    - jsonPath: .spec.args
      name: Args
      type: string
