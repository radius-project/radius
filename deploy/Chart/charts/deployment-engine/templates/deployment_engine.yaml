apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    control-plane: controller-manager
  name: bicep-controller-manager
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      labels:
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --kubernetes=true
        env:
        - name: ASPNETCORE_URLS
          value: https://+:6443
        - name: kubernetes
          value: "true"
        - name: ASPNETCORE_Kestrel__Endpoints__HttpsPemCertFile__Url
          value: https://+:6443
        - name: ASPNETCORE_Logging__LogLevel__Default
          value: "Debug"
        - name: ASPNETCORE_Kestrel__Endpoints__HttpsPemCertFile__Certificate__Path
          value: /bicep/cert/tls.crt
        - name: ASPNETCORE_Kestrel__Endpoints__HttpsPemCertFile__Certificate__KeyPath
          value: /bicep/cert/tls.key
        image: {{ .Values.image }}:{{ .Values.tag }}
        name: deployment-engine
        ports:
        - containerPort: 6443
          name: bicep-api
          protocol: TCP
        volumeMounts:
        - name: cert
          mountPath: /bicep/cert
          readOnly: true
        securityContext:
          allowPrivilegeEscalation: false
      - env:
        - name: ASPNETCORE_URLS
          value: http://+:5000
        image: {{ .Values.bicepExtensibilityImage }}:{{ .Values.bicepExtensibilityTag }}
        name: bicep-extensibility
        ports:
        - containerPort: 5000
          name: bicepext
          protocol: TCP
        securityContext:
          allowPrivilegeEscalation: false
      volumes:
      - name: cert
        secret:
          secretName: bicep-deployment-engine-cert
      terminationGracePeriodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: bicep-api
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 443
      name: https
      protocol: TCP
      targetPort: 6443
  selector:
    control-plane: controller-manager
---
apiVersion: v1
kind: Service
metadata:
  name: bicep-extensibility
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 5000
      name: http
      protocol: TCP
      targetPort: 5000
  selector:
    control-plane: controller-manager
