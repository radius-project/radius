apiVersion: v1
kind: Service
metadata:
  name: de-api
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 6443
      name: http
      protocol: TCP
      targetPort: 6443
  selector:
    control-plane: de