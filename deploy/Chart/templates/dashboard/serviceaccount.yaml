apiVersion: v1
kind: ServiceAccount
metadata:
  name: dashboard-account
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: dashboard-account
    app.kubernetes.io/part-of: radius
