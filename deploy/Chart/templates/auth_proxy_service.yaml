apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: radius-controller-manager
  name: radius-controller-manager-metrics-service
  namespace: radius-system
spec:
  ports:
  - name: https
    port: 8443
    targetPort: https
  selector:
    control-plane: radius-controller-manager