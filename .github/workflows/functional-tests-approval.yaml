name: 'Approve Functional Tests'
on:
  pull_request:
    branches:
      - main
      - features/*
      - release/*
jobs:
  save_pr_as_artifact:
      name: 'Approve Functional Tests'
      uses: radius-project/radius/.github/workflows/save-pr-as-artifact@main.yml
  # approve-functional-tests-run:
  #   name: 'Approve Functional Tests'
  #   runs-on: ubuntu-latest
  #   environment: functional-tests
  #   steps:
    # - name: Use custom actions
    #   uses: actions/checkout@v3
    # - name: Save PR number
      # uses: ./.github/actions/save-pr-as-artifact
