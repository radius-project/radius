name: 'Approve Functional Tests'
on:
  pull_request:
    branches:
      - main
      - features/*
      - release/*
jobs:
  wait_for_approval:
    environment: functional-tests
    runs-on: ubuntu-latest
  save_pr_as_artifact:
      name: 'Approve Functional Tests'
      uses: test-org-admin/radius-main/.github/workflows/save-pr-as-artifact.yaml@main
