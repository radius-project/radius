codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        informational: true
        target: auto
        threshold: 0%

comment:
  layout: "diff, flags, files"
  behavior: default
  require_changes: no

#ignore:
#  - "**/mock_*.go"            # ignore all mock files
#  - "**/armrpc/rpctest/*"     # ignore all test helpers in rpctest
#  - "**/zz_*.go"              # ignore autogenerated files from autorest
