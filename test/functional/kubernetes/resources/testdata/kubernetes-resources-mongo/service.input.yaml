apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
  namespace: kubernetes-resources-mongo
spec:
  type: ClusterIP
  ports:
  - name: tcp-mongo
    port: 27037
    protocol: TCP
    targetPort: mongo
  clusterIP: None
