apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: kubernetes-resources-redis
spec:
  type: ClusterIP
  ports:
  - name: tcp-redis
    port: 6379
    protocol: TCP
    targetPort: redis
  clusterIP: None
